import java.io.PrintWriter

import com.mdpeg._
import com.mdpeg.ASTTransform._
import com.mdpeg.OutputTransform._
import org.scalatest.{FlatSpec, Matchers}

class OutputTransformSpec  extends FlatSpec with Matchers {
  it should "transformToHtml" in {
//    val term  = scala.io.Source.fromFile("input.md").mkString
//
//    val parser = new BlockParser(term)
//    val parsed = parser.InputLine.run()
//    val rawAstTree = parsed.get
//    val transformedTree = rawAstTree |> transformTree |> (_.right.get) |> toHtml
//
//    println(transformedTree)
//    new PrintWriter("output.html") { write(transformedTree); close }
  }
}
